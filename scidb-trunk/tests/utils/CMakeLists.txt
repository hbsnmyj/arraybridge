# Feels like there should be a special subdirectory for test
# utilities.
configure_file(daemon.py "${GENERAL_OUTPUT_DIRECTORY}/daemon.py" COPYONLY)
configure_file(box_of_points.py "${GENERAL_OUTPUT_DIRECTORY}/box_of_points.py" COPYONLY)
